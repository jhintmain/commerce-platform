version: "2"

sql:
  - engine: "mysql"
    schema: "schema"
    queries: "query"
    gen:
      go:
        package: "db"
        out: "generate"
        emit_json_tags: true
        emit_interface: true